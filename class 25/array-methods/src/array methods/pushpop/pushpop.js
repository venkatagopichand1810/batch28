// push() -> add the item to the end of the array
// pop() -> it will removes the last item from the array

let mobiles = ["lg", "oppo", "vivo"];

mobiles.push("iphone"); //push adds the item at the end
console.log(mobiles)

mobiles.pop(); //removes the last element
mobiles.pop(); 
mobiles.pop(); 
console.log(mobiles)