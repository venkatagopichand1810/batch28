//array can hold muliples values at once..the index will from '0'


//let arr = [item1, item2, item3 ...........]; //array syntax

// creating the array
let numbers = [10, 20, 30, 40, 50];

// access the array elements
console.log(numbers[0]);
console.log(numbers[4]);

//modify the array elements
numbers[2] = 300;
console.log(numbers)