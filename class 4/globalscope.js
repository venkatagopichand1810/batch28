
// global scope....var, let and const

//var 
var name = "venkat"; //global scope var can be accessible any where

function showName() {
    console.log(name) //able to access inside the function
}

showName(); //calling the function
console.log(name); //outside the function

//let

// let name = "venkat"; //global scope var can be accessible any where

// function showName() {
//     console.log(name) //able to access inside the function
// }

// showName(); //calling the function
// console.log(name); //outside the function

//const


const name = "venkat"; //global scope var can be accessible any where

function showName() {
    console.log(name) //able to access inside the function
}

showName(); //calling the function
console.log(name); //outside the function